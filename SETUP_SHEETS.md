# スプレッドシート連携のセットアップ

## 全体の流れ

```
Googleスプレッドシート（講師マスタ）
        ↕ GAS Web App
人件費計算ツール（自動読み込み・計算結果送信）
```

---

## 1. スプレッドシートを作成

1. [Googleスプレッドシート](https://sheets.google.com) を新規作成
2. 名前を「人件費管理」など分かりやすく付けて保存

---

## 2. Google Apps Script を設定

1. スプレッドシートで **拡張機能** → **Apps Script** を開く
2. `Code.gs` の内容を削除し、`google-apps-script/Code.gs` の内容を全てコピー＆ペースト
3. **保存**（Ctrl+S / Cmd+S）

---

## 3. ウェブアプリとしてデプロイ

1. 右上 **デプロイ** → **新しいデプロイ**
2. 歯車アイコン → **ウェブアプリ** を選択
3. 設定:
   - **次のユーザーとして実行**: 自分
   - **アクセスできるユーザー**: 全員（匿名ユーザーを含む）
4. **デプロイ** をクリック
5. **ウェブアプリのURL** をコピー（`https://script.google.com/macros/s/.../exec` の形式）

---

## 4. 講師マスタシートを初期化

1. スプレッドシートに戻る
2. メニューバーに **「人件費ツール」** が追加されているのを確認
3. **人件費ツール** → **テンプレートを初期化** をクリック
4. 「講師マスタ」シートが作成され、サンプルデータが入力される

> ※メニューが見えない場合はページを再読み込みしてください

---

## 5. 講師データを入力

「講師マスタ」シートに実際の講師情報を入力します：

| 列 | 内容 | 例 |
|----|------|----|
| A: 講師名 | 講師の名前 | 田中 太郎 |
| B: チーム | 「日報」または「音声」 | 日報 |
| C: 時給（円/h） | 時給 | 1500 |
| D: 稼働時間（h）※任意 | 予定稼働時間 | 20 |

---

## 6. アプリにURLを登録・自動連携を有効化

1. 人件費計算ツールの計算画面を開く
2. **スプレッドシート連携の設定** を展開
3. コピーしたURLを **GAS Web App URL** に貼り付け
4. **「ページ読み込み時に講師データを自動取得」** にチェック
5. **URLを保存** をクリック

---

## 使い方

### 講師データの自動読み込み

- **GASから自動読み込み**: ボタンを押すと「講師マスタ」の内容が日報・音声チームに即時反映
- **自動取得ON**: ページを開くたびにスプレッドシートの最新データを自動で読み込む

### 計算結果の送信

- **スプレッドシートに送信**: 現在の計算結果を「人件費データ」シートに追記
- **保存時に自動送信**: チェックを入れると保存と同時にスプレッドシートにも送信

---

## シート構成

| シート名 | 用途 |
|----------|------|
| 講師マスタ | 講師名・チーム・時給を管理（アプリが読み込む） |
| 人件費データ | 月次の計算結果が蓄積される |
